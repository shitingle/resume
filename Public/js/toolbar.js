/*
var AllModule=new Array(14);
for (var i = 1; i <=14; i++) {
	AllModule[i]=i;
};

var module=$('.sign').toArray(); 
var NowModule=new Array();
for (var i = 0; i < module.length; i++) {
	console.log(module[i].innerHTML);
	NowModule[i]=module[i].innerHTML;

};
console.log(NowModule);

//得到现在页面上的模块和顺序，传给服务器
	var ajax='';
for (var i = 0; i < NowModule.length; i++) {
	//ajax

	ajax=ajax+'m'+i+'='+'p'+NowModule[i]+'&';
};
console.log('http://localhost/resumes?'+ajax);

mos= array.join(,)
//服务器,找到输出其他模块
select module from table where moduls not in (mos);
[{6,7,8,9}
//js得到json数组,输出可选的模块
document.write('<div>'+i+'</div>')

//如果被选择，点击成功ajax上传
ajax insert：modelue6,position6，module7 ,po7
//再查询
//输出[8,9]
//如果拖动完成，更新数据库位置？不可行！
//位置和模块，







//全部存cookies，最后上传服务器。
1.查询模块和位置。输出模块cookie【1,3，5,6】，可选的【其他】数组

依次输出1,3,5,6模板
2.位置就是顺序，先输出，在输出，提交的时候读取
最后拖动了，增加了，删除了 变成1,3,6,7
存数据库，模块1,3









/*

var choose=new Array(14);

for (var i = 1; i <=AllModule.length; i++) {
	if($.inArray(AllModule[i],NowModule)==-1){
		console.log(AllModule[i]);
		choose[i]=AllModule[i]
	}

	};


console.log(choose);
*/




